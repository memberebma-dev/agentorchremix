// The real, deployed backend origin. Used to build hosted-content URLs that
// this backend can actually serve (see routes/preview.ts), instead of
// pointing at domains nothing hosts.
export const BACKEND_URL = "https://dimg0s7y.backend.blink.new";
